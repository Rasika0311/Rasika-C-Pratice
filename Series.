Series.

#include<iostream>
using namespace std;
int main()
{
    int n,i,a;
    cin>>n;
    for(i=1;i<=n;i++)
    {
        if(i%2==0)
        {
            a=((i*i)-2);
        }
        else
        {
            a=((i*i)+2);
        }
        cout<<a<<endl;
    }
}
